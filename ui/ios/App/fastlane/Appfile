app_identifier("com.resistjs.heron")

# These are read from env when running in CI. Local dev: source .env first.
apple_id(ENV["APPLE_ID"] || "")
team_id(ENV["APPLE_TEAM_ID"] || "")
itc_team_id(ENV["APPLE_ITC_TEAM_ID"] || "")
