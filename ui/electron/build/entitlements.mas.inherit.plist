<?xml version="1.0" encoding="UTF-8"?>
<!--
  entitlements.mas.inherit.plist — inherit entitlements for child
  processes in the Mac App Store sandbox build. Electron spawns helper
  processes (renderer, GPU, network service); each needs to inherit
  the parent's sandbox + JIT permissions or it crashes on launch.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>com.apple.security.app-sandbox</key>
  <true/>
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.inherit</key>
  <true/>
</dict>
</plist>
