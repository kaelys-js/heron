Several Issues (you have skills for doing proper work right?):
DIRECTION: Atomic tasks. Verify after each atomic task. Re-check for anything missed and do a loop until nothing left.

- Animate cleanly when changing between light/dark/system modes
- Would it be trivial/easy to swap sqlite out for a different db? Is that neeeded at all? Any advantages?
- Review every source file to make sure (do not skip any files): Uses proper shared logging and error infrastructure AND that everything is surfaced using the shared notification system AND there is extensive logging in general

- Review Entire UX + All Flows + Simplify UX
- Automatically Apply + Auto-Monitor Job Application Updates + Respond + Setup Interview Stages/etc...?
