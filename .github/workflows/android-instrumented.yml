# H.G -- Android Espresso (instrumented) tests on a real emulator.
# Minimal smoke -- isolating the startup_failure cause.
name: Android Espresso

on:
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  emulator:
    name: Espresso smoke
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - name: Hello
        run: echo "hello"
